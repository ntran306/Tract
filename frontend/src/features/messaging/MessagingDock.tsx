/** Global messaging dock — rendered in RootLayout so it persists across every
 *  route. Ships in M3 (conversations + AI assistant tabs, Supabase Realtime).
 *  Rendering nothing for now keeps the layout contract in place without dead UI. */
export function MessagingDock() {
  return null
}
